#include "config_loader.h"
#include <iostream>

// Метод загрузки конфигурации из файла
void ConfigLoader::loadConfig(const std::string& filepath) {
    std::cout << "Загрузка конфигурации из файла: " << filepath << std::endl;

    // Заглушка: Реализация загрузки конфигурации будет добавлена позже
}
